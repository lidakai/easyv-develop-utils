chrome.runtime.onInstalled.addListener(async () => {

  // let url = chrome.runtime.getURL("src/browser_action/index.html");


  // let tab = await chrome.tab.create({ url });
  console.log(`Nice to see you`)
});

